"""Dataset Ingestion Layer.

Turns messy source dumps (JSONL, CSV, HTML) into a unified, versioned,
auditable canonical dataset for downstream pattern mining, template
generation, and validation.
"""

__version__ = "0.1.0"
